(function() {
  if (typeof Asteroids === "undefined") {
    window.Asteroids = {};
  }

  var gameView = Asteroids.gameView = function(game, ctx) {
    this.game = game;
    this.ctx = ctx;
  }

  
})();
